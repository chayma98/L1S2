#include <stdio.h>
#include <stdlib.h>
void saisir (int maxi , int TPolynome) {
int i=0 ;
int x =0 ;
int c= 0 ; int p=0 ;
for (i=1 ; i<= maxi ; i++){
    printf ("donner la puissance %d \n et le coefficient %d",p,c);
    scanf ("%d\n %d",&p,&c);
    TPolynome[p]= c;
}
void affichage (int maxi , int TPolynome){
int i = 0 ;
for (i=1 ; i<= maxi ; i++){
    printf (saisir(maxi,TPolynome[i]));
}
def calcul (int maxi , int TPolynome){
int i = 0 ;
for (i=1 ; i<= maxi ; i++){
    printf ("donner une valeur %d",x)
    scanf ("%d",& x);
    s = s + (t[i]*pow(x,i));}
  return s ;
}

int main()
{
int TPolynome[maxi];
const int maxi = 50;

}
