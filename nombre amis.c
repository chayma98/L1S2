#include <stdio.h>
#include <stdlib.h>
int somme (x) {
int i ;
int s = 0 ;
for (i=2 to i<=n , i++ ){
    if (x % i == 0 ){ s = s+ i;}
}
return s ;
}
int amis (x,y){
if somme(x) == somme(y) {
    printf ("% et % sont deux nombres amis");
}}
